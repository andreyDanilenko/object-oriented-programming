export const createFilmListExtraTemplate = (param) => (
  `<section class="films-list films-list--extra">
    <h2 class="films-list__title">${param.title}</h2>
      <div class="films-list__container"></div>
    </section>`
);
